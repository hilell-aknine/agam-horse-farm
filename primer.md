# primer.md — איפה אנחנו עכשיו

> סטטוס דק בלבד. הפירוט המלא: `dashboard\PROJECTS_STATE.md` (רשומת "Daytime — 2026-06-30 · החווה של אגם").

**מצב:** פעיל · **חי באוויר** https://agam-horse-farm.vercel.app/ · רב-משתמשים (ענן + חשבונות).

**Milestone נוכחי:** ✨ **מנת שדרוגים גדולה (2026-07-26 · חי):** (1) **6 נכסי ג'מיני חדשים** — עז, תרנגול הודו, סוס קשת בענן (גזע מיוחד בחנות 150), פוני משוטט, סוס-רץ+סוס-ישן (תנוחות לסוס חום בתנועה/לילה). הסרת-רקע אוטומטית (`tools/integrate_gemini.py`, flood-fill ששומר לבן פנימי). כפילויות ב-`assets/_gemini_extra/`. (2) **מצב אורח** — כפתור "לשחק בלי חשבון" בשער הכניסה, מקומי-בלבד, בחירה דביקה (`agam_guest`). (3) **פאנל אדמין** — `admin.html` + Edge Function `admin-users` (פרוס חי): רשימת משתמשות + איפוס-סיסמה, service_role בצד-שרת, טוקן `ADMIN_PANEL_TOKEN` ב-.secrets (401 בלי טוקן, **סיסמאות לא נחשפות**). `.vercelignore` חוסם supabase/tools/*.md מפרסום. (4) **שדרוג ויזואלי חינמי** — ACES tone mapping, צבע-שמש דינמי (שעת-זהב), 400 כוכבים, פרפרים ביום + גחליליות בלילה, נדנוד תפאורה ברוח, צל-קרקע, באנר `title_farm.png` בפתיחה, תפוח כגידול, ליטוש CSS + מסך-טעינה, מילוי אגם/הר. (5) **תיקוני UX מביקורת-הורה חיה** — הידוק מצלמה (לא בורחת לדשא ריק) + מרחק-עבודה קבוע, ✖ הגדרות חסין-גלישה + Escape, קריאוּת-לילה (רצפת-תאורה גבוהה), משוב-שגוי אדום+ירוק ברור, האגם נפתח ברמה 1, הקראת מספר בלחיצה. הכל אומת ב-Playwright דרך מצב-האורח. **פתוח:** ג'נרוט שאר גיליונות-הפרומפטים; אולי הגדלת hit-area לשדות/סוסים.

**Milestone קודם (v9):** 🛠️ **מנה גדולה של תיקונים+פיצ'רים (2026-07-02 ערב · חי, v9):** תוקן באג ה-X שלא סוגר (אמוji מונפש בלע קליק → z-index+pointer-events), בידוד-חשבונות (שמירת-אורח זלגה לחשבון חדש), ונתיבי-תמונה שבורים של חיות רקע. נוסף: **אפקט-פעולה מומחש** (אמוji ענק ליד הסוס), **הרחבת-שטח בכסף** (גדר 15→27 + שדות 9→15, טאב 🏞️), **בורר-קושי ידני** (קל/רגיל/מאתגר בהגדרות), **צפייה בחוות-חברות מתוך המפה** 🗺️, ו**מצב-עיצוב 🎨** לגרירת קישוטים (שנקנו + 17 קישוטי-חווה קבועים, נשמר ב-`decorPos`). אומת עם עכבר+מגע אמיתיים ב-Playwright/CDP.

**Milestone קודם:** 🌍 עולם חברתי — שער הרשמה/כניסה, חווה-לכל-חברה בענן, ביקור במצב-צפייה (`?visit=`). + עולם פתוח 6 אזורים (חווה/יער/אגם/כפר/הר/🎪 יריד) עם נסיעה, שבילים, דמויות, פעילויות, גילוי.

**מה עובד:** לולאת FarmVille מלאה (סוסים/שדות/חיות-משק/חנות/שדרוגים), קושי מותאם+בורר-קושי ידני, הרחבת-שטח, מצב-עיצוב לגרירת קישוטים, אפקט-פעולה, יום-לילה, מזג-אוויר, PWA, שמירת-ענן+הרשמה+בידוד, עולם-חברתי (מפה→ביקור בחברות), משימות/גלגל/מירוץ/משלוח/תחרות/צילום/הלבשה/חיות-נדירות/עץ-קסם.

**דוח להורה:** מסך בהגדרות ← "📊 דוח להורה" — דיוק כללי + דיוק לפי סוג תרגיל (בר צבעוני), הסוג החלש להמלצת-תרגול, רמה/רצף-שיא/אזורים/פעילויות. מבוסס נתוני-אמת מ-typeStats/worldStats.

**חידוד אמנות (חדש · חינם):** אניזוטרופיה מקסימלית ברינדר + מעבר PIL מקומי (UnsharpMask+contrast+color, medium) על כל 81 הציורים ששומר אלפא. כלי: `tools/enhance.py` (preview|apply). מקור שמור בהיסטוריית git.

**✅ PWA cache — תוקן מהשורש (v9):** ה-SW עבר מ-cache-first ל-**network-first** — כשיש אינטרנט תמיד מגישים את הגרסה העדכנית, מטמון רק אופליין. זה סוף לבעיית "משתמש מותקן תקוע על ישן" ששברה תיקונים כל היום. עדיין נהוג לבמפ `CACHE` ב-`sw.js` בכל שינוי (מנקה מטמון ישן ב-activate). **הערה למשתמש שכבר מותקן:** צריך רענון אחד-שניים או גלישה-בסתר כדי להתנתק מה-SW הישן הקודם.

**⚠️ אגם עצמה:** אם היא משחקת עם session אנונימי — במסך הבא היא תראה את שער הכניסה. צריך לרשום אותה פעם אחת (שם+מייל+סיסמה) באותו מכשיר — השמירה המקומית שלה "נתפסת" אוטומטית לחשבון החדש.

**ℹ️ שאריות דמו בפרויקט ה-Supabase (אומת 2026-07-02):** ה-ref `xgqetnlsesgwiypufodf` (חשבון hilelltohar@gmail.com) מכיל גם טבלאות ניהול-עובדים — אלו **שאריות הדמו הישן של ג'ף מ-2026-06-03** (113 עובדי דמו, 0 לוגים, אפס פעילות מאז). **ג'ף החי רץ בחשבון נפרד משלו** (`szplwdkskbeprvyvjqvv`) — אין בעיית בידוד לקוח. אפשר למחוק את טבלאות הדמו כשנוח (באישור הלל).

**הבא בתור (פתוח):** הלל לאמת בגלישה-בסתר שהיה מטמון · לרשום את אגם + לשלוח לינק לחברות · אופציונלי: סיבוב/מחיקה של פריט במצב-עיצוב · משימות-סיפור מרובות-שלבים · השמעת היומן בקול · עוד סוגי חשבון (כפל/שעון/כסף) · אופציונלי ייצור FAL מחדש (בתשלום, באישור).

**עלות FAL:** ~11.3₪ מתוך 30₪ (~81 ציורים).
