# primer.md — איפה אנחנו עכשיו

> סטטוס דק בלבד. הפירוט המלא: `dashboard\PROJECTS_STATE.md` (רשומת "Daytime — 2026-06-30 · החווה של אגם").

**מצב:** פעיל · **חי באוויר** https://agam-horse-farm.vercel.app/ · רב-משתמשים (ענן + חשבונות).

**Milestone נוכחי:** 🌍 **עולם חברתי (2026-07-02, ממתין ל-push):** דף הבית הוא שער כניסה (הרשמה עם שם+מייל+סיסמה / כניסה) — לכל חברה חווה משלה. כפתור "👭 החוות של החברות" מציג את כל השחקניות (טבלת `farm_profiles`), ולחיצה נכנסת לביקור בחווה של חברה במצב צפייה בלבד (`?visit=<user_id>` — באנר + חסימת כל הפעולות/שמירה). מוגן במכשיר משותף (`agam_farm_owner` ב-localStorage). **נבדק מקומית מקצה-לקצה ב-Playwright** — הרשמה, כניסה, רשימת חברות, ביקור, חזרה, RLS.

**Milestone קודם:** עולם פתוח עשיר — 6 אזורים (חווה/יער/אגם/כפר/הר/🎪 יריד) עם נסיעה, שבילים, דמויות, פעילויות, חיות ותנועה חיה; שכבת גילוי (יומן הרפתקאות, מחוון מיקום, משימות-אזור, צלילי-אזור). היריד (unlock רמה 5): גלגל-ענק מסתובב, קרוסלת סוסים, דוכני בלונים/קליעה/סוכריות.

**מה עובד:** לולאת FarmVille מלאה (סוסים/שדות/חיות-משק/חנות עם מוכר/שדרוגי-אסם), קושי מותאם, יום-לילה, מזג-אוויר, PWA, שמירת-ענן+הרשמה, משימות/גלגל/מירוץ/משלוח/תחרות/צילום/הלבשה/חיות-נדירות/עץ-קסם.

**דוח להורה:** מסך בהגדרות ← "📊 דוח להורה" — דיוק כללי + דיוק לפי סוג תרגיל (בר צבעוני), הסוג החלש להמלצת-תרגול, רמה/רצף-שיא/אזורים/פעילויות. מבוסס נתוני-אמת מ-typeStats/worldStats.

**חידוד אמנות (חדש · חינם):** אניזוטרופיה מקסימלית ברינדר + מעבר PIL מקומי (UnsharpMask+contrast+color, medium) על כל 81 הציורים ששומר אלפא. כלי: `tools/enhance.py` (preview|apply). מקור שמור בהיסטוריית git.

**⚠️ PWA cache:** ה-service worker הועלה ל-`agam-farm-v3` — כל שינוי קוד/אמנות מחייב במפ גרסה ב-`sw.js`, אחרת cache-first מגיש למשתמש מותקן את הישן לנצח.

**⚠️ אגם עצמה:** אם היא משחקת עם session אנונימי — במסך הבא היא תראה את שער הכניסה. צריך לרשום אותה פעם אחת (שם+מייל+סיסמה) באותו מכשיר — השמירה המקומית שלה "נתפסת" אוטומטית לחשבון החדש.

**⚠️ Supabase משותף עם ג'ף:** ה-ref `xgqetnlsesgwiypufodf` מכיל גם את טבלאות מערכת ניהול העובדים של ג'ף. המשחק משתמש ב-`game_saves`+`farm_profiles` בלבד, המדיניות מבודדת — אבל כדאי בהמשך להפריד לפרויקט ענן נפרד.

**הבא בתור (פתוח):** משימות-סיפור מרובות-שלבים · השמעת היומן בקול · עוד סוגי חשבון (כפל/שעון/כסף) · אופציונלי ייצור FAL מחדש לדמות רכה ספציפית (בתשלום, באישור).

**עלות FAL:** ~11.3₪ מתוך 30₪ (~81 ציורים).
